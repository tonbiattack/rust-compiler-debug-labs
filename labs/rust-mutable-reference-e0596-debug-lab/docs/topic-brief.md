# 題材メモ：可変参照不足で発生するE0596

| 項目 | 決定 |
| --- | --- |
| 契約 | ラベルへ完了接尾辞を追加する。 |
| Red | `&String`を通して`push_str`を呼ぶ。 |
| 原因 | 可変メソッドには可変借用が必要だが、共有参照しかない。 |
| 最小修正 | 引数を`&mut String`へ変更する。 |
| 回帰 | 接尾辞が追加されることを統合テストする。 |

既存のE0499とは異なり、複数借用の競合ではなく、API引数自体の可変性を扱います。
